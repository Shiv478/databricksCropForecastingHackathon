# app.yaml for Databricks Apps
# Databricks will run npm install and npm run build automatically.

command: ["npm", "run", "start"]

env:
  - name: NODE_ENV
    value: "production"
  - name: PORT
    value: "8080"
  - name: HOSTNAME
    value: "0.0.0.0"
